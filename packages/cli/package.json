{
  "name": "@skillbuddy/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "skm — manage AI agent skills across platforms from the terminal / CI",
  "bin": {
    "skm": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@skillbuddy/core": "workspace:*",
    "commander": "^13.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "catalog:",
    "vitest": "^3.0.0"
  }
}
